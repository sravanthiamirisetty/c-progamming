#include<stdio.h>
int main ()
{
int rev, rem, num,
temp;
scanf("%d", &num).
temp=num;
rev=0;
while(num != 0)
{
rem=num%10;
rev=rev*10+rem;
num=num/10;
}
if(temp == rev)
printf
("Palindrome");
else
printf("Not
Palindrome");
return 0;
}
